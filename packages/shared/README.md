# Shared Package

This folder is reserved for shared contracts, constants, and helper types that can be reused by the web app and backend.

The first likely contents will be:

- common status enums
- API payload schemas
- wallet and payout constants
- audit action names
